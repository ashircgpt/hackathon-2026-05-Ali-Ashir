import SettingsSection from "@/components/admin/SettingsSection";

export const metadata = { title: "Settings — Pizza3.14 Admin" };

export default function SettingsPage() {
  return <SettingsSection />;
}
