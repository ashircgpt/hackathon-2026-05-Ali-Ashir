import FeedbackSection from "@/components/admin/FeedbackSection";

export const metadata = { title: "Feedback Ledger — Pizza3.14 Admin" };

export default function FeedbackPage() {
  return <FeedbackSection />;
}
